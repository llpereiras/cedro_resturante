<%= form_for restaurante, :html => {class: 'well'} do |f| %>
  <legend>Dados</legend>
  <% if restaurante.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(restaurante.errors.count, "error") %> prohibited this restaurante from being saved:</h2>

      <ul>
      <% restaurante.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    
     <div class="form-group">
        
        <%= f.label :nome, "Nome do Restaurante" %>
        <%= f.text_field :nome, {class: 'form-control', placeholder: 'nome do restaurante'} %>
    </div>
    
  </div>

  <div class="actions">
    <%= f.submit 'Salvar',  {class: 'btn btn-success'} %>
  </div>
<% end %>
